# 🔒 SSL Basics

![replace_me](https://codeworks.blob.core.windows.net/public/assets/img/illustrations/placeholder.svg)

> **📖 [A Beginner's Guide to SSL](https://codeworksacademy.com/fs-student-guide/resources/wk8-9/07-SSL)**

## Questions

1. What is an SSL certificate?
SSL stands for secure sockets layer; an SSL certificate  are small data files that establish an encrypted link between a web server and a browser and ensures that all data passed between the web server and browser remain private.

2. What are the different types of certificates? What are they used for?
Encryption and validation:
    - Extended Validation: shows the padlock, HTTPS, business name, and business country in the address bar to diminish being mistaken for a spam website; valuable in showing the legitimacy of your domain from the address bar and you must prove that you are authorized to own the domain you’re submitting
    - Organized Validation: verifies that your organization and domain validation are real; offer a medium level of encryption; CA would verify who owns the domain and if the organization is operating legally; small green padlock with the company’s name following.
    - Domain Validation: offers a low level of encryption shown as a green padlock next to the URL in the address bar. This is the quickest validation you can receive, and you’ll only need a few company documents to apply; happens when you add a DNS to the CA. For this certificate, the CA will review the right of the applicant to own the domain being submitted.

Domain numbers:
    - Wildcard SSL: wildcard SSLs ensure that if you buy a certificate for one domain, you can use that same certificate for subdomains.
    - Unified Communications: show a padlock in the display; allow multiple domain names to be on the same certificate. UCCs were created to bridge communication between a single server and browser but have since expanded to include multiple domain names by the same owner.

3. What are some benefits of using SSL certificates?
SSLs help to provide security and legitimacy to your website. They ensure a safe connection to host private/sensitive data. Having an SSL can also increase your website's rank (SEO).

## Afternoon Assignment Link

**[Repo](https://github.com/KarinnaGorrono/DaVinki)**

Identify at least 1 takeaway from today's work
Today was a good example of adapting to changes in an original plan or workflow and working forward from setbacks.
